<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color fondo -->
    <solid android:color="@color/fondoTextField" />
    <!-- Color borde-->
    <stroke
        android:width="1dp"
        android:color="@color/bordeTextField" />
    <!-- Bordes redondeados -->
    <corners android:radius="70dp" />
</shape>
