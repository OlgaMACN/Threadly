<resources>

    <style name="Base.Theme.Threadly" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Para que la aplicación se ejecute a pantalla completa -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="Theme.Threadly" parent="Base.Theme.Threadly" />
</resources>